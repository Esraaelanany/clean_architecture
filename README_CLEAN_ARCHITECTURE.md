# مثال عداد باستخدام Clean Architecture

هذا المشروع يوضح كيفية تطبيق Clean Architecture في Flutter باستخدام مثال بسيط لعداد.

## 🏗️ بنية المشروع

```
lib/
├── core/
│   └── di/
│       └── injection_container.dart    # إدارة التبعيات
├── domain/
│   ├── entities/
│   │   └── counter.dart                 # كيان العداد
│   ├── repositories/
│   │   └── counter_repository.dart     # واجهة المستودع
│   └── usecases/
│       ├── get_counter.dart            # حالة الاستخدام للحصول على العداد
│       ├── increment_counter.dart      # حالة الاستخدام لزيادة العداد
│       ├── decrement_counter.dart      # حالة الاستخدام لتقليل العداد
│       └── reset_counter.dart          # حالة الاستخدام لإعادة تعيين العداد
├── data/
│   ├── datasources/
│   │   └── counter_local_datasource.dart # مصدر البيانات المحلي
│   └── repositories/
│       └── counter_repository_impl.dart # تنفيذ المستودع
├── presentation/
│   ├── bloc/
│   │   ├── counter_bloc.dart           # BLoC للعداد
│   │   ├── counter_event.dart          # أحداث العداد
│   │   └── counter_state.dart          # حالات العداد
│   ├── pages/
│   │   └── counter_page.dart          # صفحة العداد الرئيسية
│   └── widgets/
│       ├── counter_display.dart       # ويدجت عرض العداد
│       └── counter_buttons.dart       # ويدجت أزرار التحكم
└── main.dart                          # نقطة البداية
```

## 🎯 طبقات Clean Architecture

### 1. Domain Layer (طبقة المجال)
- **Entities**: تمثل الكائنات الأساسية في التطبيق
- **Use Cases**: تحتوي على منطق العمل الخاص بالتطبيق
- **Repository Interfaces**: تحدد واجهات المستودعات

### 2. Data Layer (طبقة البيانات)
- **Data Sources**: مصادر البيانات (محلية، API، قاعدة بيانات)
- **Repository Implementations**: تنفيذ واجهات المستودعات

### 3. Presentation Layer (طبقة العرض)
- **BLoC**: إدارة الحالة والمنطق
- **Pages**: الصفحات الرئيسية
- **Widgets**: المكونات القابلة لإعادة الاستخدام

## 🚀 الميزات

- ✅ **فصل الاهتمامات**: كل طبقة لها مسؤولية محددة
- ✅ **اختبار سهل**: يمكن اختبار كل طبقة بشكل منفصل
- ✅ **قابلية الصيانة**: كود منظم وسهل الفهم
- ✅ **قابلية التوسع**: يمكن إضافة ميزات جديدة بسهولة
- ✅ **Dependency Injection**: إدارة التبعيات باستخدام GetIt
- ✅ **State Management**: إدارة الحالة باستخدام BLoC
- ✅ **Local Storage**: حفظ البيانات محلياً باستخدام SharedPreferences

## 🛠️ التقنيات المستخدمة

- **Flutter**: إطار العمل الأساسي
- **BLoC**: إدارة الحالة
- **GetIt**: إدارة التبعيات
- **Equatable**: مقارنة الكائنات
- **SharedPreferences**: التخزين المحلي

## 📱 كيفية الاستخدام

1. **تحميل العداد**: اضغط على "تحميل العداد" لعرض القيمة المحفوظة
2. **زيادة العداد**: اضغط على الزر الأخضر (+) لزيادة القيمة
3. **تقليل العداد**: اضغط على الزر الأحمر (-) لتقليل القيمة
4. **إعادة التعيين**: اضغط على "إعادة تعيين" لإرجاع القيمة إلى الصفر

## 🔧 التثبيت والتشغيل

```bash
# تثبيت التبعيات
flutter pub get

# تشغيل التطبيق
flutter run
```

## 📚 فوائد Clean Architecture

1. **الاستقلالية**: كل طبقة مستقلة عن الأخرى
2. **المرونة**: يمكن تغيير التنفيذ دون تأثير على الطبقات الأخرى
3. **الاختبار**: سهولة كتابة الاختبارات لكل طبقة
4. **القابلية للصيانة**: كود منظم وواضح
5. **قابلية التوسع**: إضافة ميزات جديدة بسهولة

## 🎨 تصميم الواجهة

- واجهة مستخدم بسيطة وواضحة
- ألوان مميزة للأزرار المختلفة
- عرض حالة التحميل
- معالجة الأخطاء وعرضها للمستخدم
- دعم اللغة العربية

هذا المثال يوضح كيفية تطبيق Clean Architecture بشكل صحيح في Flutter، مما يجعل الكود أكثر تنظيماً وقابلية للصيانة والتطوير.
